<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="mobile-web-app-capable" content="yes">
    <title>QR Connect - Connect With Us</title>
    <link rel="icon" type="image/png" href="/images/qrconnect logo.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #000000;
            min-height: 100vh;
            min-height: -webkit-fill-available;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 15px;
            margin: 0;
            -webkit-tap-highlight-color: transparent;
        }

        .container {
            text-align: center;
            max-width: 600px;
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            flex: 1;
            min-height: calc(100vh - 30px);
        }

        /* Mobile Navigation */
        .mobile-nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            background: rgba(0, 0, 0, 0.95);
            backdrop-filter: blur(10px);
            padding: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .mobile-nav-logo {
            max-width: 120px;
            height: auto;
            max-height: 50px;
        }

        .hamburger {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
            padding: 8px;
            -webkit-tap-highlight-color: transparent;
            touch-action: manipulation;
        }

        .hamburger span {
            width: 25px;
            height: 3px;
            background: #ffffff;
            border-radius: 2px;
            transition: all 0.3s ease;
        }

        .hamburger.active span:nth-child(1) {
            transform: rotate(45deg) translate(8px, 8px);
        }

        .hamburger.active span:nth-child(2) {
            opacity: 0;
        }

        .hamburger.active span:nth-child(3) {
            transform: rotate(-45deg) translate(7px, -7px);
        }

        .mobile-menu {
            position: fixed;
            top: 70px;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.98);
            z-index: 999;
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            padding: 30px 20px;
            overflow-y: auto;
        }

        .mobile-menu.active {
            transform: translateX(0);
        }

        .mobile-menu-links {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .mobile-menu-link {
            color: #ffffff;
            text-decoration: none;
            font-size: 18px;
            font-weight: 500;
            padding: 15px;
            border-radius: 8px;
            transition: all 0.3s ease;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .mobile-menu-link:hover,
        .mobile-menu-link:active {
            background: rgba(255, 255, 255, 0.1);
            transform: translateX(5px);
        }

        .logo-section {
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            padding-top: 15px;
            padding-bottom: 10px;
        }

        .adx-logo {
            max-width: 180px;
            width: 60%;
            height: auto;
            max-height: 120px;
        }

        .icons-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 30px;
            width: 100%;
            margin: auto 0;
            flex: 1;
            padding: 20px 10px;
        }

        .icons-row {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 35px;
            flex-wrap: wrap;
            width: 100%;
        }

        .icons-row.top-row {
            /* Top row: 3 icons */
        }

        .icons-row.bottom-row {
            /* Bottom row: 2 icons - centered */
            justify-content: center;
        }

        .icon-link {
            color: #ffffff;
            text-decoration: none;
            transition: all 0.2s ease;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
            padding: 12px;
            min-width: 70px;
            min-height: 70px;
            justify-content: center;
            -webkit-tap-highlight-color: rgba(255, 255, 255, 0.1);
            touch-action: manipulation;
            -webkit-touch-callout: none;
            user-select: none;
        }

        .icon-link:hover {
            transform: scale(1.1);
            opacity: 0.8;
        }

        .icon-link:active {
            transform: scale(0.95);
            opacity: 0.7;
        }

        .icon-link i {
            font-size: 42px;
            transition: all 0.2s ease;
            pointer-events: none;
        }

        /* Instagram - gradient icon */
        .icon-link.instagram i {
            background: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Payment - green */
        .icon-link.payment i {
            color: #25D366;
        }

        /* WhatsApp - green */
        .icon-link.whatsapp i {
            color: #25D366;
        }

        /* Website - blue */
        .icon-link.website i {
            color: #4285F4;
        }

        /* Google - red/orange */
        .icon-link.google i {
            color: #EA4335;
        }

        /* Mobile First - Base styles for mobile */
        @media (max-width: 480px) {
            body {
                padding: 10px;
                padding-top: 80px; /* Space for mobile nav */
            }

            .mobile-nav {
                display: flex;
            }

            .hamburger {
                display: flex;
            }

            .logo-section {
                display: none; /* Hide logo section on mobile, show in nav */
            }

            .container {
                min-height: calc(100vh - 20px);
            }

            .adx-logo {
                max-width: 140px;
                width: 50%;
                max-height: 100px;
            }

            .icons-container {
                gap: 25px;
                padding: 15px 5px;
            }

            .icons-row {
                gap: 20px;
                justify-content: space-around;
                width: 100%;
            }

            .icon-link {
                min-width: 65px;
                min-height: 65px;
                padding: 12px;
                flex: 0 0 auto;
            }

            .icon-link i {
                font-size: 40px;
            }

            .footer {
                padding-top: 30px;
                padding-bottom: 15px;
                font-size: 11px;
            }

            .footer-logo {
                height: 16px;
            }

            .footer-content {
                gap: 6px;
            }
        }

        /* Small mobile devices */
        @media (max-width: 360px) {
            .icons-row {
                gap: 15px;
            }

            .icon-link {
                min-width: 60px;
                min-height: 60px;
                padding: 10px;
            }

            .icon-link i {
                font-size: 36px;
            }
        }

        /* Tablet */
        @media (min-width: 481px) and (max-width: 768px) {
            .icons-container {
                gap: 40px;
            }

            .icon-link {
                min-width: 75px;
                min-height: 75px;
            }

            .icon-link i {
                font-size: 44px;
            }
        }

        /* Desktop */
        @media (min-width: 769px) {
            .icons-container {
                gap: 50px;
            }

            .icon-link {
                min-width: 80px;
                min-height: 80px;
            }

            .icon-link i {
                font-size: 48px;
            }
        }

        /* Large Desktop */
        @media (min-width: 1024px) {
            .icon-link {
                min-width: 90px;
                min-height: 90px;
            }

            .icon-link i {
                font-size: 52px;
            }
        }

        .footer {
            padding-top: 30px;
            padding-bottom: 15px;
            text-align: center;
            color: #ffffff;
            font-size: 13px;
            width: 100%;
        }

        .footer-content {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            flex-wrap: wrap;
        }

        .footer-logo {
            height: 24px;
            width: auto;
            transition: opacity 0.3s ease;
            cursor: pointer;
        }

        .footer-logo:hover {
            opacity: 0.7;
        }

        .footer-text {
            color: #ffffff;
            text-decoration: none;
        }

        .footer-link {
            display: flex;
            align-items: center;
            gap: 8px;
            text-decoration: none;
            color: #ffffff;
            transition: opacity 0.3s ease;
        }

        .footer-link:hover {
            opacity: 0.7;
        }

    </style>
</head>
<body>
    <!-- Mobile Navigation -->
    <nav class="mobile-nav">
        <img src="/images/adxlogo.jpeg" alt="ADX Transit" class="mobile-nav-logo">
        <div class="hamburger" id="hamburger" onclick="toggleMobileMenu()">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </nav>

    <!-- Mobile Menu -->
    <div class="mobile-menu" id="mobileMenu">
        <div class="mobile-menu-links">
            <a href="/login" class="mobile-menu-link" onclick="closeMobileMenu()">Signup</a>
            <a href="#pricing" class="mobile-menu-link" onclick="closeMobileMenu()">Pricing</a>
            <a href="#features" class="mobile-menu-link" onclick="closeMobileMenu()">Features</a>
        </div>
    </div>

    <div class="container">
        <div class="logo-section">
            <img src="/images/adxlogo.jpeg" alt="ADX Transit" class="adx-logo">
        </div>
        
        <div class="icons-container">
            <!-- Top row: 3 icons (Olympic style) -->
            <div class="icons-row top-row">
                <a href="/instagram" class="icon-link instagram" target="_blank">
                    <i class="fab fa-instagram"></i>
                </a>
                
                <a href="/payment" class="icon-link payment">
                    <i class="fas fa-credit-card"></i>
                </a>
                
                <a href="/whatsapp" class="icon-link whatsapp" target="_blank">
                    <i class="fab fa-whatsapp"></i>
                </a>
            </div>
            
            <!-- Bottom row: 2 icons (centered) -->
            <div class="icons-row bottom-row">
                <a href="/website" class="icon-link website" target="_blank">
                    <i class="fas fa-globe"></i>
                </a>
                
                <a href="/google" class="icon-link google" target="_blank">
                    <i class="fab fa-google"></i>
                </a>
            </div>
        </div>
        
        <div class="footer" style="position: relative;">
            <div class="footer-content">
                <span>Built by</span>
                <a href="https://aideveloperindia.store" class="footer-link" target="_blank" style="display: flex; align-items: center; gap: 8px; text-decoration: none; color: #ffffff;">
                    <img src="/images/A-logo-transparent.png" alt="AI Developer India" style="height: 20px; width: auto; display: block;">
                    <span>AI Developer India</span>
                </a>
            </div>
            <div class="footer-stats" id="footerStats" style="position: absolute; bottom: 20px; right: 20px; display: flex; flex-direction: column; gap: 8px; text-align: right; z-index: 10;">
                <div class="footer-stat-item" style="color: #ffffff; font-size: 14px;">
                    <span class="stat-label" style="font-size: 12px; opacity: 0.7; margin-right: 6px;">Users:</span>
                    <span class="stat-value" id="footerUsersCount" style="font-weight: 600; color: #4CAF50;">-</span>
                </div>
                <div class="footer-stat-item" style="color: #ffffff; font-size: 14px;">
                    <span class="stat-label" style="font-size: 12px; opacity: 0.7; margin-right: 6px;">Clients:</span>
                    <span class="stat-value" id="footerClientsCount" style="font-weight: 600; color: #4CAF50;">-</span>
                </div>
            </div>
        </div>
        <script>
            async function loadFooterStats() {
                try {
                    const response = await fetch('/p/api/settings');
                    const data = await response.json();
                    if (data.success) {
                        const usersEl = document.getElementById('footerUsersCount');
                        const clientsEl = document.getElementById('footerClientsCount');
                        if (usersEl) usersEl.textContent = data.usersCount.toLocaleString('en-IN') || '0';
                        if (clientsEl) clientsEl.textContent = data.clientsCount.toLocaleString('en-IN') || '0';
                    }
                } catch (error) {
                    const usersEl = document.getElementById('footerUsersCount');
                    const clientsEl = document.getElementById('footerClientsCount');
                    if (usersEl) usersEl.textContent = '0';
                    if (clientsEl) clientsEl.textContent = '0';
                }
            }
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', loadFooterStats);
            } else {
                loadFooterStats();
            }
        </script>
    </div>

    <script>
        function toggleMobileMenu() {
            const menu = document.getElementById('mobileMenu');
            const hamburger = document.getElementById('hamburger');
            menu.classList.toggle('active');
            hamburger.classList.toggle('active');
        }

        function closeMobileMenu() {
            const menu = document.getElementById('mobileMenu');
            const hamburger = document.getElementById('hamburger');
            menu.classList.remove('active');
            hamburger.classList.remove('active');
        }

        // Close menu when clicking outside
        document.addEventListener('click', function(event) {
            const menu = document.getElementById('mobileMenu');
            const hamburger = document.getElementById('hamburger');
            const isClickInsideMenu = menu.contains(event.target);
            const isClickOnHamburger = hamburger.contains(event.target);
            
            if (!isClickInsideMenu && !isClickOnHamburger && menu.classList.contains('active')) {
                closeMobileMenu();
            }
        });

        // Close menu on escape key
        document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
                closeMobileMenu();
            }
        });
    </script>
</body>
</html>
