<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="icon" type="image/png" href="/images/qrconnect logo.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #0a0a0a;
            color: #ffffff;
            min-height: 100vh;
            padding: 15px;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            flex-wrap: wrap;
            gap: 10px;
        }

        .header h1 {
            font-size: 24px;
            font-weight: 600;
        }

        .logout-btn {
            background: #ff4444;
            color: white;
            padding: 8px 16px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            font-size: 14px;
            transition: background 0.3s;
        }

        .logout-btn:hover {
            background: #cc0000;
        }

        .section {
            background: #1a1a1a;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .section-title {
            font-size: 18px;
            margin-bottom: 15px;
            font-weight: 600;
        }

        .profile-form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        label {
            color: #ccc;
            font-size: 13px;
            font-weight: 500;
        }

        input[type="text"],
        input[type="tel"],
        input[type="file"] {
            padding: 10px 12px;
            border: 1px solid #333;
            border-radius: 8px;
            background: #0a0a0a;
            color: #fff;
            font-size: 14px;
            max-width: 100%;
        }

        input[type="file"] {
            cursor: pointer;
        }

        .preview-img {
            max-width: 120px;
            max-height: 120px;
            border: 1px solid #333;
            border-radius: 50%;
            padding: 5px;
            margin-top: 8px;
            object-fit: cover;
        }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s;
            width: 100%;
            max-width: 200px;
            margin: 0 auto;
        }

        .btn-primary {
            background: #4285F4;
            color: white;
        }

        .btn-primary:hover {
            background: #3367d6;
        }

        .btn-success {
            background: #25D366;
            color: white;
        }

        .btn-success:hover {
            background: #1da851;
        }

        .btn-danger {
            background: #ff4444;
            color: white;
        }

        .btn-danger:hover {
            background: #cc0000;
        }

        .payment-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            text-align: center;
            padding: 20px;
        }

        .status-badge {
            display: inline-block;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .status-paid {
            background: #25D366;
            color: white;
        }

        .status-pending {
            background: #ff9800;
            color: white;
        }

        .alert {
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 15px;
            font-size: 14px;
        }

        .alert-success {
            background: #25D366;
            color: white;
        }

        .alert-error {
            background: #ff4444;
            color: white;
        }

        .alert-info {
            background: #4285F4;
            color: white;
        }

        .links-list {
            display: grid;
            gap: 12px;
        }

        .link-item {
            background: rgba(66, 133, 244, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            padding: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 15px;
        }

        .link-info {
            flex: 1;
            min-width: 0;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .link-icon {
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            flex-shrink: 0;
        }

        .link-info strong {
            display: block;
            color: #fff;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .link-info span {
            color: #888;
            font-size: 12px;
            word-break: break-all;
        }

        .link-actions {
            display: flex;
            gap: 8px;
            flex-shrink: 0;
        }

        .btn-small {
            padding: 6px 12px;
            font-size: 12px;
            max-width: none;
            width: auto;
        }

        .add-link-form {
            background: #0a0a0a;
            border: 2px solid #333;
            border-radius: 12px;
            padding: 20px;
            margin-top: 15px;
            display: none;
        }

        .add-link-form.active {
            display: block;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 15px;
            margin-bottom: 15px;
        }

        .category-selector {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
            gap: 10px;
            margin: 15px 0;
        }

        .category-option {
            padding: 12px;
            border: 2px solid rgba(255, 255, 255, 0.2);
            border-radius: 8px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
            background: rgba(255, 255, 255, 0.05);
        }

        .category-option:hover {
            border-color: #4285F4;
            background: rgba(66, 133, 244, 0.1);
        }

        .category-option.selected {
            border-color: #4285F4;
            background: linear-gradient(135deg, #4285F4, #25D366);
        }

        .category-option i {
            font-size: 24px;
            display: block;
            margin-bottom: 6px;
            color: #fff;
        }

        .category-option span {
            font-size: 11px;
            color: #fff;
            font-weight: 500;
        }

        .payment-apps {
            display: flex;
            justify-content: center;
            gap: 12px;
            flex-wrap: wrap;
            margin-top: 15px;
        }

        .payment-app-btn {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background: rgba(255, 255, 255, 0.08);
            border: 2px solid rgba(255, 255, 255, 0.2);
            border-radius: 12px;
            padding: 15px 20px;
            cursor: pointer;
            transition: all 0.3s ease;
            color: #fff;
            min-width: 90px;
        }

        .payment-app-btn:hover {
            background: rgba(255, 255, 255, 0.15);
            border-color: rgba(255, 255, 255, 0.4);
            transform: translateY(-3px);
        }

        .payment-icon {
            margin-bottom: 8px;
        }

        .payment-icon svg {
            width: 40px;
            height: 40px;
        }

        .payment-label {
            font-size: 12px;
            font-weight: 600;
        }

        @media (min-width: 480px) {
            .form-grid {
                grid-template-columns: 1fr 1fr;
            }
        }

        @media (min-width: 768px) {
            body {
                padding: 20px;
            }

            .container {
                max-width: 700px;
            }

            .link-item {
                flex-direction: row;
            }

            .link-actions {
                width: auto;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Dashboard</h1>
            <a href="/auth/logout" class="logout-btn">Logout</a>
        </div>

        <div id="alert-container"></div>

        <!-- Profile Section -->
        <div class="section">
            <h2 class="section-title">Business Profile</h2>
            <form class="profile-form" id="profileForm">
                <div class="form-group">
                    <label>Business Name</label>
                    <input type="text" name="businessName" value="<%= user.businessName || '' %>" placeholder="Business Name">
                </div>
                <div class="form-group">
                    <label>Phone Number</label>
                    <input type="tel" name="phoneNumber" value="<%= user.phoneNumber || '' %>" placeholder="+91 9876543210">
                </div>
                <div class="form-group">
                    <label>Address</label>
                    <input type="text" name="address" value="<%= user.address || '' %>" placeholder="Address">
                </div>
                <div class="form-group">
                    <label>Business Logo</label>
                    <input type="file" name="logo" accept="image/*">
                    <% if (user.logo && user.logo.trim() !== '') { %>
                        <img src="<%= user.logo %>" alt="Logo" class="preview-img">
                    <% } %>
                </div>
                <div class="form-group">
                    <label>Dynamic QR Code</label>
                    <input type="file" name="uploadedQrCode" accept="image/*">
                    <% if (user.uploadedQrCode && user.uploadedQrCode.trim() !== '') { %>
                        <img src="<%= user.uploadedQrCode %>" alt="Dynamic QR" class="preview-img">
                    <% } %>
                </div>
                <div class="form-group">
                    <label>Payment QR Code</label>
                    <input type="file" name="bankQrCode" accept="image/*">
                    <% if (user.bankQrCode && user.bankQrCode.trim() !== '') { %>
                        <img src="<%= user.bankQrCode %>" alt="Payment QR" class="preview-img">
                    <% } %>
                </div>
                <button type="submit" class="btn btn-primary">Update</button>
            </form>
        </div>

        <!-- Payment/Subscription Section -->
        <div class="section payment-section">
            <h2 class="section-title" style="margin-bottom: 10px;">Subscription</h2>
            <% 
            const now = new Date();
            const isSubscriptionActive = user.subscriptionActive && user.subscriptionEndDate && new Date(user.subscriptionEndDate) > now;
            const subscriptionEndDate = user.subscriptionEndDate ? new Date(user.subscriptionEndDate).toLocaleDateString('en-IN') : null;
            const hasLinks = links && links.length > 0;
            %>
            <% if (isSubscriptionActive) { %>
                <p><span class="status-badge status-paid">Active</span></p>
                <p style="margin-top: 10px; color: #ddd; font-size: 14px;">Until: <strong><%= subscriptionEndDate %></strong></p>
            <% } else { %>
                <p><span class="status-badge status-pending">Inactive</span></p>
                <div style="margin-top: 15px;">
                    <p style="color: #fff; font-size: 20px; font-weight: bold;">₹999</p>
                    <p style="color: #ddd; font-size: 14px; margin-top: 5px;">per year</p>
                </div>
                <button class="btn btn-success" onclick="showPaymentApps()" style="margin-top: 15px;">
                    Pay ₹999
                </button>
                <div class="payment-apps" id="paymentApps" style="display: none; margin-top: 15px;">
                    <button class="payment-app-btn payment-googlepay" onclick="openUPIPayment()">
                        <div class="payment-icon">
                            <svg viewBox="0 0 24 24">
                                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                            </svg>
                        </div>
                        <span class="payment-label">Google Pay</span>
                    </button>
                    <button class="payment-app-btn payment-phonepe" onclick="openUPIPayment()">
                        <div class="payment-icon">
                            <svg viewBox="0 0 24 24">
                                <rect fill="#5F259F" x="2" y="2" width="20" height="20" rx="4"/>
                                <path fill="#FFFFFF" d="M12 7c-2.76 0-5 2.24-5 5s2.24 5 5 5 5-2.24 5-5-2.24-5-5-5zm0 8c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3z"/>
                            </svg>
                        </div>
                        <span class="payment-label">PhonePe</span>
                    </button>
                    <button class="payment-app-btn payment-paytm" onclick="openUPIPayment()">
                        <div class="payment-icon">
                            <svg viewBox="0 0 24 24">
                                <rect fill="#00BAF2" x="2" y="2" width="20" height="20" rx="4"/>
                                <path fill="#FFFFFF" d="M12 7c-2.76 0-5 2.24-5 5s2.24 5 5 5 5-2.24 5-5-2.24-5-5-5zm0 8c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3z"/>
                            </svg>
                        </div>
                        <span class="payment-label">Paytm</span>
                    </button>
                </div>
            <% } %>
        </div>

        <!-- QR Code Section -->
        <div class="section">
            <h2 class="section-title">QR Code</h2>
            <div id="qr-container">
                <% 
                const accountActivated = user.accountActivated || false;
                %>
                <% if (!hasLinks) { %>
                    <p style="color: #888; text-align: center; padding: 15px;">Add links to generate QR code</p>
                <% } else if (!isSubscriptionActive) { %>
                    <p style="color: #888; text-align: center; padding: 15px;">Subscribe to generate QR code</p>
                <% } else if (!accountActivated) { %>
                    <p style="color: #888; text-align: center; padding: 15px;">Pending approval</p>
                <% } else { %>
                    <button class="btn btn-primary" onclick="generateQR()" id="generateQRBtn">
                        <i class="fas fa-qrcode"></i> Generate
                    </button>
                <% } %>
            </div>
            <p style="margin-top: 15px; color: #888; font-size: 12px; text-align: center;">
                <a href="/p/<%= user.uniqueSlug %>" target="_blank" style="color: #4285F4; text-decoration: none;">/p/<%= user.uniqueSlug %></a>
            </p>
        </div>

        <!-- Links Section -->
        <div class="section">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px; flex-wrap: wrap; gap: 10px;">
                <h2 class="section-title" style="margin: 0;">Links</h2>
                <button class="btn btn-primary btn-small" onclick="toggleAddLinkForm()" style="margin: 0;">
                    <i class="fas fa-plus"></i> Add
                </button>
            </div>
            
            <div class="add-link-form" id="addLinkForm">
                <h3 style="margin-bottom: 15px; font-size: 16px;">Add Link</h3>
                <form id="linkForm">
                    <input type="hidden" id="linkId" name="linkId">
                    
                    <div class="form-group">
                        <label>Category</label>
                        <div class="category-selector" id="categorySelector">
                            <% Object.keys(defaultCategories).forEach(cat => { %>
                                <% if (cat !== 'payment') { %>
                                    <div class="category-option" data-category="<%= cat %>" data-type="default">
                                        <i class="<%= defaultCategories[cat].icon %>"></i>
                                        <span><%= defaultCategories[cat].name %></span>
                                    </div>
                                <% } %>
                            <% }); %>
                            <div class="category-option" data-category="custom" data-type="custom">
                                <i class="fas fa-plus"></i>
                                <span>Custom</span>
                            </div>
                        </div>
                    </div>

                    <div class="form-grid">
                        <div class="form-group">
                            <label>Name</label>
                            <input type="text" id="displayName" name="displayName" required placeholder="Display Name">
                        </div>
                        <div class="form-group">
                            <label>URL</label>
                            <input type="url" id="url" name="url" required placeholder="https://...">
                        </div>
                    </div>
                    
                    <div class="form-group" id="customIconGroup" style="display: none;">
                        <label>Icon</label>
                        <input type="file" id="customIcon" name="customIcon" accept="image/*">
                    </div>

                    <div style="display: flex; gap: 10px; margin-top: 15px;">
                        <button type="submit" class="btn btn-primary" style="flex: 1; max-width: none;">
                            Save
                        </button>
                        <button type="button" class="btn btn-danger btn-small" onclick="cancelAddLink()">
                            Cancel
                        </button>
                    </div>
                </form>
            </div>

            <div class="links-list" id="linksList">
                <% if (links && links.length > 0) { %>
                    <% links.forEach(link => { %>
                        <% if (link.category !== 'payment') { %>
                            <div class="link-item" data-link-id="<%= link._id %>">
                                <div class="link-info">
                                    <div class="link-icon">
                                        <% if (link.categoryType === 'custom' && (link.icon.startsWith('data:image') || link.icon.startsWith('/'))) { %>
                                            <img src="<%= link.icon %>" style="width: 20px; height: 20px;">
                                        <% } else { %>
                                            <i class="<%= link.icon %>"></i>
                                        <% } %>
                                    </div>
                                    <div>
                                        <strong><%= link.displayName %></strong>
                                        <span><%= link.url %></span>
                                    </div>
                                </div>
                                <div class="link-actions">
                                    <button class="btn btn-primary btn-small" onclick="editLink('<%= link._id %>', '<%= link.category %>', '<%= link.categoryType %>', '<%= link.url %>', '<%= link.displayName %>')">Edit</button>
                                    <button class="btn btn-danger btn-small" onclick="deleteLink('<%= link._id %>')">Delete</button>
                                </div>
                            </div>
                        <% } %>
                    <% }); %>
                <% } else { %>
                    <p style="color: #888; text-align: center; padding: 15px;">No links added</p>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
    <script>
        let selectedCategory = null;
        let selectedCategoryType = null;
        
        document.querySelectorAll('.category-option').forEach(option => {
            option.addEventListener('click', function() {
                document.querySelectorAll('.category-option').forEach(opt => opt.classList.remove('selected'));
                this.classList.add('selected');
                selectedCategory = this.dataset.category;
                selectedCategoryType = this.dataset.type;
                
                const customIconGroup = document.getElementById('customIconGroup');
                if (selectedCategoryType === 'custom') {
                    customIconGroup.style.display = 'block';
                    document.getElementById('customIcon').required = true;
                } else {
                    customIconGroup.style.display = 'none';
                    document.getElementById('customIcon').required = false;
                }
            });
        });

        document.getElementById('profileForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const logoInput = e.target.querySelector('input[name="logo"]');
            const qrInput = e.target.querySelector('input[name="bankQrCode"]');
            const dynamicQrInput = e.target.querySelector('input[name="uploadedQrCode"]');
            const maxSize = 100 * 1024;
            
            if (logoInput && logoInput.files[0] && logoInput.files[0].size > maxSize) {
                showAlert('Logo too large. Max 100KB', 'error');
                return;
            }
            
            if (qrInput && qrInput.files[0] && qrInput.files[0].size > maxSize) {
                showAlert('QR code too large. Max 100KB', 'error');
                return;
            }

            if (dynamicQrInput && dynamicQrInput.files[0] && dynamicQrInput.files[0].size > maxSize) {
                showAlert('QR code too large. Max 100KB', 'error');
                return;
            }
            
            const formData = new FormData(e.target);
            
            try {
                const response = await fetch('/dashboard/update-profile', {
                    method: 'POST',
                    body: formData,
                    credentials: 'include'
                });
                
                if (!response.ok) {
                    const errorData = await response.json().catch(() => ({ message: 'Request failed' }));
                    throw new Error(errorData.message || `HTTP error! status: ${response.status}`);
                }
                
                const data = await response.json();
                if (data.success) {
                    showAlert('Profile updated', 'success');
                    setTimeout(() => location.reload(), 1000);
                } else {
                    showAlert(data.message || 'Error updating profile', 'error');
                }
            } catch (error) {
                console.error('Profile update error:', error);
                showAlert(error.message || 'Error updating profile', 'error');
            }
        });

        document.getElementById('linkForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            if (!selectedCategory) {
                showAlert('Select a category', 'error');
                return;
            }
            
            if (selectedCategoryType === 'custom') {
                const customIconFile = document.getElementById('customIcon').files[0];
                if (customIconFile && customIconFile.size > 100 * 1024) {
                    showAlert('Icon too large. Max 100KB', 'error');
                    return;
                }
            }

            const formData = new FormData();
            formData.append('category', selectedCategory);
            formData.append('categoryType', selectedCategoryType);
            formData.append('url', document.getElementById('url').value);
            formData.append('displayName', document.getElementById('displayName').value);
            
            const linkId = document.getElementById('linkId').value;
            if (linkId) formData.append('linkId', linkId);
            
            if (selectedCategoryType === 'custom') {
                const fileInput = document.getElementById('customIcon');
                if (fileInput.files[0]) {
                    formData.append('customIcon', fileInput.files[0]);
                }
            }

            const submitBtn = e.target.querySelector('button[type="submit"]');
            if (submitBtn) {
                submitBtn.disabled = true;
                submitBtn.innerHTML = 'Saving...';
            }

            try {
                const response = await fetch('/dashboard/link', {
                    method: 'POST',
                    body: formData,
                    credentials: 'include'
                });
                
                const data = await response.json();
                if (data.success) {
                    showAlert('Link saved', 'success');
                    cancelAddLink();
                    setTimeout(() => location.reload(), 1000);
                } else {
                    showAlert(data.message || 'Error saving link', 'error');
                    if (submitBtn) {
                        submitBtn.disabled = false;
                        submitBtn.innerHTML = 'Save';
                    }
                }
            } catch (error) {
                showAlert('Error saving link', 'error');
                if (submitBtn) {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = 'Save';
                }
            }
        });

        function toggleAddLinkForm() {
            const form = document.getElementById('addLinkForm');
            form.classList.toggle('active');
            if (form.classList.contains('active')) {
                resetLinkForm();
            }
        }

        function cancelAddLink() {
            document.getElementById('addLinkForm').classList.remove('active');
            resetLinkForm();
        }

        function resetLinkForm() {
            document.getElementById('linkForm').reset();
            document.getElementById('linkId').value = '';
            selectedCategory = null;
            selectedCategoryType = null;
            document.querySelectorAll('.category-option').forEach(opt => opt.classList.remove('selected'));
            document.getElementById('customIconGroup').style.display = 'none';
        }

        function editLink(id, category, type, url, displayName) {
            toggleAddLinkForm();
            document.getElementById('linkId').value = id;
            document.getElementById('url').value = url;
            document.getElementById('displayName').value = displayName;
            
            const categoryOption = document.querySelector(`[data-category="${category}"][data-type="${type}"]`);
            if (categoryOption) {
                categoryOption.click();
            }
        }

        async function deleteLink(id) {
            if (!confirm('Delete this link?')) return;
            
            try {
                const response = await fetch(`/dashboard/link/${id}`, {
                    method: 'DELETE',
                    credentials: 'include'
                });
                
                if (!response.ok) {
                    const errorData = await response.json().catch(() => ({ message: 'Request failed' }));
                    throw new Error(errorData.message || `HTTP error! status: ${response.status}`);
                }
                
                const data = await response.json();
                if (data.success) {
                    showAlert('Link deleted', 'success');
                    setTimeout(() => location.reload(), 1000);
                } else {
                    showAlert(data.message || 'Error deleting link', 'error');
                }
            } catch (error) {
                console.error('Delete link error:', error);
                showAlert(error.message || 'Error deleting link', 'error');
            }
        }

        function showPaymentApps() {
            const paymentApps = document.getElementById('paymentApps');
            paymentApps.style.display = paymentApps.style.display === 'none' ? 'flex' : 'none';
        }
        
        async function openUPIPayment() {
            try {
                const upiResponse = await fetch('/payment/upi-url', {
                    credentials: 'include'
                });
                
                if (!upiResponse.ok) {
                    throw new Error('Failed to get payment URL');
                }
                
                const upiData = await upiResponse.json();
                
                if (upiData.success && upiData.upiUrl) {
                    window.location.href = upiData.upiUrl;
                    
                    setTimeout(() => {
                        if (confirm('Payment completed?')) {
                            confirmPayment();
                        }
                    }, 2000);
                } else {
                    throw new Error('Payment URL not available');
                }
            } catch (error) {
                console.error('Payment error:', error);
                showAlert(error.message || 'Error opening payment', 'error');
            }
        }
        
        async function confirmPayment() {
            try {
                showAlert('Processing...', 'info');
                
                const response = await fetch('/payment/pay', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    credentials: 'include'
                });
                
                if (!response.ok) {
                    const errorData = await response.json().catch(() => ({ message: 'Request failed' }));
                    throw new Error(errorData.message || `HTTP error! status: ${response.status}`);
                }
                
                const data = await response.json();
                
                if (data.success) {
                    const endDate = data.subscriptionEndDate ? new Date(data.subscriptionEndDate).toLocaleDateString('en-IN') : '1 year from now';
                    showAlert(`Payment successful! Active until ${endDate}`, 'success');
                    setTimeout(() => location.reload(), 2000);
                } else {
                    showAlert(data.message || 'Payment confirmation failed', 'error');
                }
            } catch (error) {
                console.error('Payment confirmation error:', error);
                showAlert(error.message || 'Error confirming payment', 'error');
            }
        }

        async function generateQR() {
            const btn = document.getElementById('generateQRBtn');
            if (btn) {
                btn.disabled = true;
                btn.innerHTML = 'Generating...';
            }
            
            try {
                const response = await fetch('/qr/generate', {
                    credentials: 'include'
                });
                
                if (!response.ok) {
                    const errorData = await response.json().catch(() => ({ message: 'Request failed' }));
                    throw new Error(errorData.message || `HTTP error! status: ${response.status}`);
                }
                
                const data = await response.json();
                
                if (data.success) {
                    const qrContainer = document.getElementById('qr-container');
                    qrContainer.innerHTML = `
                        <div style="text-align: center;">
                            <div style="margin-bottom: 15px;">
                                <img src="${data.qrUrl}" alt="QR Code" style="max-width: 250px; width: 100%; border-radius: 8px;">
                            </div>
                            <a href="${data.qrUrl}" download="qr-code.png" class="btn btn-primary">
                                <i class="fas fa-download"></i> Download
                            </a>
                        </div>
                    `;
                    showAlert('QR code generated', 'success');
                } else {
                    showAlert(data.message || 'Error generating QR code', 'error');
                    if (btn) {
                        btn.disabled = false;
                        btn.innerHTML = '<i class="fas fa-qrcode"></i> Generate';
                    }
                }
            } catch (error) {
                console.error('QR generation error:', error);
                showAlert(error.message || 'Error generating QR code', 'error');
                if (btn) {
                    btn.disabled = false;
                    btn.innerHTML = '<i class="fas fa-qrcode"></i> Generate';
                }
            }
        }

        function showAlert(message, type) {
            const container = document.getElementById('alert-container');
            const alert = document.createElement('div');
            alert.className = `alert alert-${type}`;
            alert.textContent = message;
            container.appendChild(alert);
            
            setTimeout(() => {
                alert.remove();
            }, 4000);
        }
    </script>
</body>
</html>
