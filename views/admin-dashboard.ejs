<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Dynamic QR Generator</title>
    <link rel="icon" type="image/png" href="/images/qrconnect logo.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #0a0a0a;
            color: #ffffff;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            flex-wrap: wrap;
            gap: 15px;
        }

        .header h1 {
            font-size: 28px;
        }

        .logout-btn {
            background: #ff4444;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: #1a1a1a;
            border-radius: 12px;
            padding: 25px;
            text-align: center;
        }

        .stat-card h3 {
            color: #888;
            font-size: 14px;
            margin-bottom: 10px;
            text-transform: uppercase;
        }

        .stat-card .value {
            font-size: 36px;
            font-weight: bold;
            color: #4CAF50;
        }

        .section {
            background: #1a1a1a;
            border-radius: 12px;
            padding: 25px;
            margin-bottom: 25px;
        }

        .section-title {
            font-size: 20px;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #333;
        }

        th {
            color: #888;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 12px;
        }

        td {
            color: #fff;
        }

        .badge {
            padding: 4px 12px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 600;
        }

        .badge-active {
            background: #4CAF50;
            color: white;
        }

        .badge-inactive {
            background: #ff4444;
            color: white;
        }

        .pagination {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-top: 20px;
        }

        .pagination button {
            padding: 8px 16px;
            background: #2a2a2a;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
        }

        .pagination button:hover {
            background: #3a3a3a;
        }

        .pagination button:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        /* Mobile First - Base styles for mobile */
        @media (max-width: 480px) {
            body {
                padding: 10px;
            }

            .container {
                max-width: 100%;
            }

            .header {
                flex-direction: column;
                align-items: flex-start;
                gap: 10px;
            }

            .header h1 {
                font-size: 22px;
            }

            .stats-grid {
                grid-template-columns: 1fr;
                gap: 15px;
            }

            table {
                font-size: 11px;
                display: block;
                overflow-x: auto;
                -webkit-overflow-scrolling: touch;
            }

            th, td {
                padding: 8px 6px;
                white-space: nowrap;
            }

            .btn {
                padding: 10px 16px;
                font-size: 13px;
            }

            .pagination {
                flex-wrap: wrap;
                gap: 5px;
            }

            .pagination button {
                padding: 8px 12px;
                font-size: 12px;
            }
        }

        @media (max-width: 768px) {
            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            table {
                font-size: 12px;
            }

            th, td {
                padding: 8px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Admin Dashboard</h1>
            <a href="/auth/logout" class="logout-btn">Logout</a>
        </div>

        <div class="stats-grid" id="statsGrid">
            <div class="stat-card">
                <h3>Total Clients</h3>
                <div class="value" id="totalClients">-</div>
            </div>
            <div class="stat-card">
                <h3>Active Subscriptions</h3>
                <div class="value" id="activeSubscriptions">-</div>
            </div>
            <div class="stat-card">
                <h3>Total Links</h3>
                <div class="value" id="totalLinks">-</div>
            </div>
            <div class="stat-card">
                <h3>QR Codes Generated</h3>
                <div class="value" id="totalQRCodes">-</div>
            </div>
            <div class="stat-card">
                <h3>Total Revenue</h3>
                <div class="value" id="totalRevenue">₹-</div>
            </div>
            <div class="stat-card">
                <h3>New Clients (30d)</h3>
                <div class="value" id="recentClients">-</div>
            </div>
        </div>

        <div class="section" style="margin-bottom: 25px;">
            <h2 class="section-title">User Feedback Statistics</h2>
            <div class="stats-grid" id="feedbackStatsGrid">
                <div class="stat-card" style="background: linear-gradient(135deg, #4CAF50, #45a049);">
                    <h3>Very Useful</h3>
                    <div class="value" id="veryUsefulCount" style="color: #fff;">-</div>
                </div>
                <div class="stat-card" style="background: linear-gradient(135deg, #4285F4, #357ae8);">
                    <h3>Good Idea</h3>
                    <div class="value" id="goodIdeaCount" style="color: #fff;">-</div>
                </div>
                <div class="stat-card" style="background: linear-gradient(135deg, #ff4444, #e63939);">
                    <h3>Not Useful</h3>
                    <div class="value" id="notUsefulCount" style="color: #fff;">-</div>
                </div>
                <div class="stat-card">
                    <h3>Total Feedback</h3>
                    <div class="value" id="totalFeedback">-</div>
                </div>
            </div>
        </div>

        <div class="section">
            <h2 class="section-title">All Clients</h2>
            <div id="clientsTable">
                <p>Loading...</p>
            </div>
        </div>
    </div>

    <script>
        let currentPage = 1;

        async function loadStats() {
            try {
                const response = await fetch('/admin/stats');
                const data = await response.json();
                
                if (data.success) {
                    document.getElementById('totalClients').textContent = data.stats.totalClients;
                    document.getElementById('activeSubscriptions').textContent = data.stats.activeSubscriptions;
                    document.getElementById('totalLinks').textContent = data.stats.totalLinks;
                    document.getElementById('totalQRCodes').textContent = data.stats.totalQRCodes;
                    document.getElementById('totalRevenue').textContent = `₹${data.stats.totalRevenue.toLocaleString('en-IN')}`;
                    document.getElementById('recentClients').textContent = data.stats.recentClients;
                    
                    // Load feedback statistics
                    if (data.stats.feedback) {
                        document.getElementById('veryUsefulCount').textContent = data.stats.feedback.veryUseful || 0;
                        document.getElementById('goodIdeaCount').textContent = data.stats.feedback.goodIdea || 0;
                        document.getElementById('notUsefulCount').textContent = data.stats.feedback.notUseful || 0;
                        document.getElementById('totalFeedback').textContent = data.stats.feedback.total || 0;
                    }
                }
            } catch (error) {
                console.error('Error loading stats:', error);
            }
        }

        async function loadClients(page = 1) {
            try {
                const response = await fetch(`/admin/clients?page=${page}`);
                const data = await response.json();
                
                if (data.success) {
                    const table = document.getElementById('clientsTable');
                    
                    if (data.clients.length === 0) {
                        table.innerHTML = '<p>No clients found.</p>';
                        return;
                    }

                    let html = `
                        <table>
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>Business</th>
                                    <th>Phone</th>
                                    <th>Paid</th>
                                    <th>Subscription</th>
                                    <th>Activated</th>
                                    <th>Feedback</th>
                                    <th>Joined</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                    `;

                    data.clients.forEach(client => {
                        const isActive = client.subscriptionActive && 
                                       new Date(client.subscriptionEndDate) > new Date();
                        const endDate = client.subscriptionEndDate ? 
                                       new Date(client.subscriptionEndDate).toLocaleDateString('en-IN') : 'N/A';
                        const isPaid = client.paymentCompleted || false;
                        const isActivated = client.accountActivated || false;
                        
                        // Feedback stats
                        const feedback = client.feedback || { veryUseful: 0, goodIdea: 0, notUseful: 0, total: 0 };
                        const feedbackText = feedback.total > 0 
                            ? `VU: ${feedback.veryUseful}, GI: ${feedback.goodIdea}, NU: ${feedback.notUseful}`
                            : 'No feedback';
                        
                        html += `
                            <tr>
                                <td>${client.name || 'N/A'}</td>
                                <td>${client.email}</td>
                                <td>${client.businessName || 'N/A'}</td>
                                <td>${client.phoneNumber || 'N/A'}</td>
                                <td>
                                    <span class="badge ${isPaid ? 'badge-active' : 'badge-inactive'}">
                                        ${isPaid ? 'Paid' : 'Not Paid'}
                                    </span>
                                </td>
                                <td>
                                    <span class="badge ${isActive ? 'badge-active' : 'badge-inactive'}">
                                        ${isActive ? `Active (${endDate})` : 'Inactive'}
                                    </span>
                                </td>
                                <td>
                                    <span class="badge ${isActivated ? 'badge-active' : 'badge-inactive'}">
                                        ${isActivated ? 'Yes' : 'No'}
                                    </span>
                                </td>
                                <td style="font-size: 11px; color: #aaa;">
                                    ${feedbackText}
                                </td>
                                <td>${new Date(client.createdAt).toLocaleDateString('en-IN')}</td>
                                <td>
                                    <button onclick="viewClient('${client._id}')" style="padding: 6px 12px; background: #4285F4; color: white; border: none; border-radius: 4px; cursor: pointer;">View</button>
                                </td>
                            </tr>
                        `;
                    });

                    html += `
                            </tbody>
                        </table>
                    `;

                    // Pagination
                    if (data.pagination.pages > 1) {
                        html += `
                            <div class="pagination">
                                <button onclick="loadClients(${data.pagination.page - 1})" 
                                        ${data.pagination.page === 1 ? 'disabled' : ''}>Previous</button>
                                <span style="padding: 8px 16px;">Page ${data.pagination.page} of ${data.pagination.pages}</span>
                                <button onclick="loadClients(${data.pagination.page + 1})" 
                                        ${data.pagination.page === data.pagination.pages ? 'disabled' : ''}>Next</button>
                            </div>
                        `;
                    }

                    table.innerHTML = html;
                    currentPage = page;
                }
            } catch (error) {
                console.error('Error loading clients:', error);
                document.getElementById('clientsTable').innerHTML = '<p>Error loading clients.</p>';
            }
        }

        function viewClient(id) {
            window.location.href = `/admin/clients/${id}`;
        }

        // Load data on page load
        loadStats();
        loadClients(1);

        // Refresh stats every 30 seconds
        setInterval(loadStats, 30000);
    </script>

    <div style="margin-top: 40px; padding-top: 20px; border-top: 1px solid #333; text-align: center; color: #666; font-size: 12px;">
        <div style="display: flex; align-items: center; justify-content: center; gap: 8px;">
            <span>Built by</span>
            <a href="https://aideveloperindia.store" target="_blank" style="display: flex; align-items: center; gap: 8px; text-decoration: none; color: #888;">
                <img src="/images/A-logo-transparent.png" alt="AI Developer India" style="height: 20px; width: auto; display: block;">
                <span>AI Developer India</span>
            </a>
        </div>
    </div>
</body>
</html>

