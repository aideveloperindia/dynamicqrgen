<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="mobile-web-app-capable" content="yes">
    <title><%= user.businessName || user.name %> - Connect With Us</title>
    <link rel="icon" type="image/png" href="/images/qrconnect logo.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #000000;
            min-height: 100vh;
            min-height: -webkit-fill-available;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 15px;
            margin: 0;
            -webkit-tap-highlight-color: transparent;
        }

        .container {
            text-align: center;
            max-width: 600px;
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            flex: 1;
            min-height: calc(100vh - 30px);
        }

        .logo-section {
            width: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding-top: 15px;
            padding-bottom: 10px;
            gap: 15px;
        }
        
        .business-name-section {
            text-align: center;
        }

        .business-logo {
            max-width: 180px;
            width: 60%;
            height: auto;
            max-height: 120px;
            object-fit: contain;
            border-radius: 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        .default-logo {
            max-width: 180px;
            width: 60%;
            height: auto;
            max-height: 120px;
            border-radius: 20px;
        }

        .icons-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 30px;
            width: 100%;
            margin: auto 0;
            flex: 1;
            padding: 20px 10px;
        }

        .icons-row {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 35px;
            flex-wrap: wrap;
            width: 100%;
        }

        .icons-row.bottom-row {
            justify-content: center;
        }

        .icon-link {
            color: #ffffff;
            text-decoration: none;
            transition: all 0.2s ease;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
            padding: 10px;
            min-width: 60px;
            min-height: 60px;
            justify-content: center;
            -webkit-tap-highlight-color: rgba(255, 255, 255, 0.1);
        }

        .icon-link:hover {
            transform: scale(1.1);
            opacity: 0.8;
        }

        .icon-link:active {
            transform: scale(0.95);
            opacity: 0.7;
        }

        .icon-link i {
            font-size: 42px;
            transition: all 0.2s ease;
        }

        .icon-link img {
            width: 42px;
            height: 42px;
            object-fit: contain;
        }

        /* Instagram - gradient icon */
        .icon-link.instagram i {
            background: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Payment - green */
        .icon-link.payment i {
            color: #25D366;
        }

        /* WhatsApp - green */
        .icon-link.whatsapp i {
            color: #25D366;
        }

        /* Website - blue */
        .icon-link.website i {
            color: #4285F4;
        }

        /* Google - red/orange */
        .icon-link.google i {
            color: #EA4335;
        }

        /* Facebook - blue */
        .icon-link.facebook i {
            color: #1877F2;
        }

        /* YouTube - red */
        .icon-link.youtube i {
            color: #FF0000;
        }

        /* Twitter - blue */
        .icon-link.twitter i {
            color: #1DA1F2;
        }

        /* LinkedIn - blue */
        .icon-link.linkedin i {
            color: #0077B5;
        }

        /* Menu - red */
        .icon-link.menu i {
            color: #FF6B6B;
        }

        /* Mobile First */
        @media (max-width: 480px) {
            body {
                padding: 10px;
            }

            .container {
                min-height: calc(100vh - 20px);
            }

            .logo-section {
                padding-top: 10px;
                padding-bottom: 5px;
            }

            .business-logo, .default-logo {
                max-width: 140px;
                width: 50%;
                max-height: 100px;
            }

            .icons-container {
                gap: 20px;
                padding: 15px 5px;
            }

            .icons-row {
                gap: 25px;
            }

            .icon-link {
                min-width: 50px;
                min-height: 50px;
                padding: 8px;
            }

            .icon-link i, .icon-link img {
                font-size: 36px;
                width: 36px;
                height: 36px;
            }

            .footer {
                padding-top: 30px;
                padding-bottom: 15px;
                font-size: 12px;
            }

            .footer-logo {
                height: 18px;
            }
        }

        /* Tablet */
        @media (min-width: 481px) and (max-width: 768px) {
            .icons-container {
                gap: 40px;
            }

            .icon-link i, .icon-link img {
                font-size: 40px;
                width: 40px;
                height: 40px;
            }
        }

        /* Desktop */
        @media (min-width: 769px) {
            .icons-container {
                gap: 50px;
            }

            .icon-link i, .icon-link img {
                font-size: 48px;
                width: 48px;
                height: 48px;
            }
        }

        .footer {
            padding-top: 30px;
            padding-bottom: 15px;
            text-align: center;
            color: #ffffff;
            font-size: 13px;
            width: 100%;
        }

        .footer-content {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            flex-wrap: wrap;
        }

        .footer-logo {
            height: 24px;
            width: auto;
            transition: opacity 0.3s ease;
            cursor: pointer;
        }

        .footer-logo:hover {
            opacity: 0.7;
        }

        .footer-text {
            color: #ffffff;
            text-decoration: none;
        }

        .footer-link {
            display: flex;
            align-items: center;
            gap: 8px;
            text-decoration: none;
            color: #ffffff;
            transition: opacity 0.3s ease;
        }

        .footer-link:hover {
            opacity: 0.7;
        }
        
        .download-qr-section {
            width: 100%;
            text-align: center;
            padding: 30px 15px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            margin: 20px 0;
        }
        
        .qr-preview {
            margin: 15px 0;
        }
        
        .download-btn {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: linear-gradient(135deg, #4285F4, #25D366);
            color: white;
            padding: 12px 24px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            margin-top: 15px;
        }
        
        .download-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(66, 133, 244, 0.4);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo-section">
            <% if (user.logo) { %>
                <img src="<%= user.logo %>" alt="<%= user.businessName || user.name %>" class="business-logo">
            <% } %>
            <div class="business-name-section">
                <h2 style="color: #fff; font-size: 24px; margin: 0; font-weight: 600;"><%= user.businessName || user.name %></h2>
                <% if (user.phoneNumber && user.phoneNumber.trim() !== '') { %>
                    <a href="tel:<%= user.phoneNumber.replace(/\s+/g, '').replace(/[^0-9+]/g, '') %>" class="call-button" style="display: inline-flex; align-items: center; gap: 8px; margin-top: 15px; padding: 12px 24px; background: #25D366; color: white; text-decoration: none; border-radius: 25px; font-weight: 600; transition: all 0.3s ease; font-size: 16px;">
                        <i class="fas fa-phone"></i> Call <%= user.phoneNumber %>
                    </a>
                <% } %>
            </div>
        </div>
        
        <div class="icons-container">
            <% 
            const linksArray = links || [];
            const topRow = linksArray.slice(0, 3);
            const bottomRow = linksArray.slice(3);
            %>
            
            <% if (topRow.length > 0) { %>
            <div class="icons-row top-row">
                <% topRow.forEach((link, index) => { %>
                    <% 
                    // Check if this is a UPI payment link
                    const isUPILink = link.url && (
                        link.url.toLowerCase().startsWith('upi://') ||
                        link.url.toLowerCase().startsWith('upiqr://') ||
                        link.url.toLowerCase().includes('upi://') ||
                        link.url.toLowerCase().includes('pay?pa=') ||
                        link.url.toLowerCase().includes('pay?pn=')
                    );
                    %>
                    <a href="/p/<%= user.uniqueSlug %>/redirect/<%= link._id %>" 
                       class="icon-link <%= link.category %>" 
                       <% if (isUPILink) { %>data-upi-link="true"<% } %>
                       onclick="<% if (isUPILink) { %>return handleUPILink(event, '<%= link.url.replace(/'/g, "\\'") %>');<% } else { %>return true;<% } %>">
                        <% if (link.categoryType === 'custom' && (link.icon.startsWith('data:image') || link.icon.startsWith('/'))) { %>
                            <img src="<%= link.icon %>" alt="<%= link.displayName %>">
                        <% } else { %>
                            <i class="<%= link.icon %>"></i>
                        <% } %>
                    </a>
                <% }); %>
            </div>
            <% } %>
            
            <% if (bottomRow.length > 0) { %>
            <div class="icons-row bottom-row">
                <% bottomRow.forEach((link) => { %>
                    <% 
                    // Check if this is a UPI payment link
                    const isUPILink = link.url && (
                        link.url.toLowerCase().startsWith('upi://') ||
                        link.url.toLowerCase().startsWith('upiqr://') ||
                        link.url.toLowerCase().includes('upi://') ||
                        link.url.toLowerCase().includes('pay?pa=') ||
                        link.url.toLowerCase().includes('pay?pn=')
                    );
                    %>
                    <a href="/p/<%= user.uniqueSlug %>/redirect/<%= link._id %>" 
                       class="icon-link <%= link.category %>" 
                       <% if (isUPILink) { %>data-upi-link="true"<% } %>
                       onclick="<% if (isUPILink) { %>return handleUPILink(event, '<%= link.url.replace(/'/g, "\\'") %>');<% } else { %>return true;<% } %>">
                        <% if (link.categoryType === 'custom' && (link.icon.startsWith('data:image') || link.icon.startsWith('/'))) { %>
                            <img src="<%= link.icon %>" alt="<%= link.displayName %>">
                        <% } else { %>
                            <i class="<%= link.icon %>"></i>
                        <% } %>
                    </a>
                <% }); %>
            </div>
            <% } %>
        </div>
        
        <!-- Download QR Code Section -->
        <div class="download-qr-section">
            <h3 style="color: #fff; font-size: 18px; margin-bottom: 15px; font-weight: 600;">Download QR Code</h3>
            <div class="qr-preview">
                <% if (user.qrCode) { %>
                    <img src="<%= user.qrCode %>" alt="QR Code" id="qrCodeImage" style="max-width: 200px; width: 100%; border-radius: 12px; background: white; padding: 10px;">
                <% } else { %>
                    <div style="padding: 20px; background: rgba(255,255,255,0.1); border-radius: 12px;">
                        <i class="fas fa-spinner fa-spin" style="font-size: 24px; color: #fff;"></i>
                        <p style="color: #fff; margin-top: 10px; font-size: 14px;">Generating QR Code...</p>
                    </div>
                <% } %>
            </div>
            <a href="/p/<%= user.uniqueSlug %>/qr-code" download="qr-code-<%= user.uniqueSlug %>.png" class="download-btn" id="downloadQrBtn">
                <i class="fas fa-download"></i> Download QR Code
            </a>
            <p style="color: #888; font-size: 12px; margin-top: 10px; text-align: center;">
                Same QR code as printed - generated on-demand
            </p>
        </div>
        
        <script>
            // Handle UPI payment links - opens UPI app selector on mobile
            function handleUPILink(event, url) {
                event.preventDefault();
                
                // Extract UPI URL if it's embedded in a regular URL
                let upiUrl = url;
                
                // If it's a regular URL containing UPI parameters, extract the UPI part
                if (upiUrl.includes('upi://')) {
                    // Extract complete UPI URL with all parameters
                    const upiMatch = upiUrl.match(/upi:\/\/[^\s"']+/i);
                    if (upiMatch) {
                        upiUrl = decodeURIComponent(upiMatch[0]);
                    }
                } else if (upiUrl.includes('pay?pa=') || upiUrl.includes('pay?pn=') || upiUrl.includes('upi://pay')) {
                    // Convert HTTP UPI link to upi:// protocol, preserving ALL parameters
                    try {
                        // Try to parse as URL first
                        let urlObj;
                        try {
                            urlObj = new URL(upiUrl);
                        } catch (e) {
                            // If not a valid URL, try to extract from upi:// format
                            if (upiUrl.includes('upi://')) {
                                const match = upiUrl.match(/upi:\/\/([^'"\s]+)/i);
                                if (match) {
                                    upiUrl = 'upi://' + match[1];
                                    // Already in correct format, use as-is
                                    openUPIApp(upiUrl);
                                    return false;
                                }
                            }
                            throw new Error('Invalid URL format');
                        }
                        
                        const params = new URLSearchParams(urlObj.search);
                        
                        // Extract all UPI parameters (preserve ALL including aid, tn, etc.)
                        const pa = params.get('pa') || params.get('upi');
                        const pn = params.get('pn') || 'Merchant';
                        const am = params.get('am') || '';
                        const cu = params.get('cu') || 'INR';
                        const aid = params.get('aid') || ''; // App ID for merchant payments (removes ₹2000 limit)
                        const tn = params.get('tn') || ''; // Transaction note
                        const tr = params.get('tr') || ''; // Transaction reference
                        
                        if (pa) {
                            // Build UPI URL preserving ALL parameters
                            let upiParams = `pa=${encodeURIComponent(pa)}&pn=${encodeURIComponent(pn)}`;
                            if (am) upiParams += `&am=${am}`;
                            if (cu) upiParams += `&cu=${cu}`;
                            if (aid) upiParams += `&aid=${encodeURIComponent(aid)}`; // Critical for merchant payments
                            if (tn) upiParams += `&tn=${encodeURIComponent(tn)}`;
                            if (tr) upiParams += `&tr=${encodeURIComponent(tr)}`;
                            
                            upiUrl = `upi://pay?${upiParams}`;
                        }
                    } catch (e) {
                        console.error('Error parsing UPI URL:', e);
                        // If parsing fails, try to use URL as-is
                    }
                }
                
                // Open UPI app with improved method
                openUPIApp(upiUrl);
                
                return false;
            }
            
            // Improved function to open UPI app and show app selector
            function openUPIApp(upiUrl) {
                // Method 1: Direct location change (works on most mobile browsers)
                try {
                    window.location.href = upiUrl;
                } catch (e) {
                    console.log('Method 1 failed, trying method 2');
                }
                
                // Method 2: Create a hidden link and click it (better compatibility)
                setTimeout(function() {
                    try {
                        const link = document.createElement('a');
                        link.href = upiUrl;
                        link.style.display = 'none';
                        document.body.appendChild(link);
                        link.click();
                        document.body.removeChild(link);
                    } catch (e) {
                        console.log('Method 2 failed, trying method 3');
                    }
                }, 100);
                
                // Method 3: window.open as fallback
                setTimeout(function() {
                    try {
                        window.open(upiUrl, '_blank');
                    } catch (e) {
                        console.error('All methods failed to open UPI link');
                        // Show manual link
                        if (confirm('Unable to open payment app automatically. Click OK to copy the payment link.')) {
                            navigator.clipboard.writeText(upiUrl).then(() => {
                                alert('Payment link copied! Paste it in your UPI app.');
                            });
                        }
                    }
                }, 300);
            }
            
            // Also handle UPI links on page load (for direct navigation)
            document.addEventListener('DOMContentLoaded', function() {
                const upiLinks = document.querySelectorAll('[data-upi-link="true"]');
                upiLinks.forEach(function(link) {
                    link.addEventListener('click', function(e) {
                        const url = this.getAttribute('href');
                        // Extract URL from href if needed
                        if (url.includes('/redirect/')) {
                            // Let the server handle it, but we'll intercept on client side
                            return true;
                        }
                    });
                });
            });
        </script>
        
        <div class="footer">
            <div class="footer-content">
                <span>Built by</span>
                <a href="https://aideveloperindia.store" class="footer-link" target="_blank">
                    <img src="/images/A-logo-transparent.png" alt="AI Developer India" class="footer-logo">
                    <span class="footer-text">AI Developer India</span>
                </a>
            </div>
            <div style="margin-top: 15px;">
                <a href="/terms" class="footer-link" style="font-size: 12px; opacity: 0.7;">Terms & Conditions</a>
            </div>
        </div>
    </div>
</body>
</html>

