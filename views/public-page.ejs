<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="mobile-web-app-capable" content="yes">
    <title><%= user.businessName || user.name %></title>
    <link rel="icon" type="image/png" href="/images/qrconnect logo.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #000000;
            min-height: 100vh;
            min-height: -webkit-fill-available;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 15px;
            margin: 0;
            -webkit-tap-highlight-color: transparent;
        }

        .container {
            text-align: center;
            max-width: 500px;
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            flex: 1;
            min-height: calc(100vh - 30px);
        }

        .logo-section {
            width: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding-top: 15px;
            padding-bottom: 10px;
            gap: 15px;
        }

        .business-logo {
            max-width: 150px;
            width: 50%;
            height: auto;
            max-height: 100px;
            object-fit: cover;
            border-radius: 50%;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        }

        .default-logo {
            max-width: 150px;
            width: 50%;
            height: auto;
            max-height: 100px;
            border-radius: 50%;
        }

        .business-name {
            color: #fff;
            font-size: 22px;
            font-weight: 600;
            margin: 0;
        }

        .call-button {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-top: 12px;
            padding: 10px 20px;
            background: #25D366;
            color: white;
            text-decoration: none;
            border-radius: 20px;
            font-weight: 600;
            font-size: 14px;
            transition: all 0.3s ease;
        }

        .call-button:hover {
            background: #1da851;
            transform: translateY(-2px);
        }

        .icons-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 25px;
            width: 100%;
            margin: auto 0;
            flex: 1;
            padding: 20px 10px;
        }

        .icons-row {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 30px;
            flex-wrap: wrap;
            width: 100%;
        }

        .icon-link {
            color: #ffffff;
            text-decoration: none;
            transition: all 0.2s ease;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 6px;
            padding: 8px;
            min-width: 55px;
            min-height: 55px;
            justify-content: center;
            -webkit-tap-highlight-color: rgba(255, 255, 255, 0.1);
        }

        .icon-link:hover {
            transform: scale(1.1);
            opacity: 0.8;
        }

        .icon-link:active {
            transform: scale(0.95);
            opacity: 0.7;
        }

        .icon-link i {
            font-size: 38px;
            transition: all 0.2s ease;
        }

        .icon-link img {
            width: 38px;
            height: 38px;
            object-fit: contain;
        }

        .icon-link.instagram i {
            background: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .icon-link.whatsapp i {
            color: #25D366;
        }

        .icon-link.website i {
            color: #4285F4;
        }

        .icon-link.google i {
            color: #EA4335;
        }

        .icon-link.facebook i {
            color: #1877F2;
        }

        .icon-link.youtube i {
            color: #FF0000;
        }

        .icon-link.twitter i {
            color: #1DA1F2;
        }

        .icon-link.linkedin i {
            color: #0077B5;
        }

        .icon-link.menu i {
            color: #FF6B6B;
        }

        .qr-download-section {
            width: 100%;
            text-align: center;
            padding: 20px 15px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 16px;
            margin: 20px 0;
        }

        .qr-buttons {
            display: flex;
            flex-direction: column;
            gap: 12px;
            margin-top: 15px;
        }

        .download-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background: linear-gradient(135deg, #4285F4, #25D366);
            color: white;
            padding: 12px 20px;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            font-size: 14px;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            width: 100%;
            max-width: 200px;
            margin: 0 auto;
        }

        .download-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(66, 133, 244, 0.4);
        }

        .download-btn i {
            font-size: 16px;
        }

        .footer {
            padding-top: 25px;
            padding-bottom: 15px;
            text-align: center;
            color: #ffffff;
            font-size: 12px;
            width: 100%;
        }

        .footer-content {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            flex-wrap: wrap;
        }

        .footer-logo {
            height: 20px;
            width: auto;
            transition: opacity 0.3s ease;
        }

        .footer-link {
            display: flex;
            align-items: center;
            gap: 8px;
            text-decoration: none;
            color: #ffffff;
            transition: opacity 0.3s ease;
        }

        .footer-link:hover {
            opacity: 0.7;
        }

        @media (max-width: 480px) {
            body {
                padding: 10px;
            }

            .container {
                min-height: calc(100vh - 20px);
            }

            .logo-section {
                padding-top: 10px;
                padding-bottom: 5px;
            }

            .business-logo, .default-logo {
                max-width: 120px;
                width: 45%;
                max-height: 80px;
                border-radius: 50%;
            }

            .business-name {
                font-size: 20px;
            }

            .icons-container {
                gap: 20px;
                padding: 15px 5px;
            }

            .icons-row {
                gap: 25px;
            }

            .icon-link {
                min-width: 50px;
                min-height: 50px;
                padding: 6px;
            }

            .icon-link i, .icon-link img {
                font-size: 32px;
                width: 32px;
                height: 32px;
            }

            .qr-download-section {
                padding: 15px 10px;
            }

            .download-btn {
                padding: 10px 18px;
                font-size: 13px;
            }
        }

        @media (min-width: 481px) and (max-width: 768px) {
            .icons-container {
                gap: 30px;
            }

            .icon-link i, .icon-link img {
                font-size: 36px;
                width: 36px;
                height: 36px;
            }
        }

        @media (min-width: 769px) {
            .icons-container {
                gap: 40px;
            }

            .icon-link i, .icon-link img {
                font-size: 42px;
                width: 42px;
                height: 42px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo-section">
            <% if (user.logo) { %>
                <img src="<%= user.logo %>" alt="<%= user.businessName || user.name %>" class="business-logo">
            <% } %>
            <h2 class="business-name"><%= user.businessName || user.name %></h2>
            <% if (user.phoneNumber && user.phoneNumber.trim() !== '') { %>
                <a href="tel:<%= user.phoneNumber.replace(/\s+/g, '').replace(/[^0-9+]/g, '') %>" class="call-button">
                    <i class="fas fa-phone"></i> Call
                </a>
            <% } %>
        </div>
        
        <div class="icons-container">
            <% 
            const linksArray = links || [];
            // Filter out payment category links
            const nonPaymentLinks = linksArray.filter(link => link.category !== 'payment');
            const topRow = nonPaymentLinks.slice(0, 3);
            const bottomRow = nonPaymentLinks.slice(3);
            %>
            
            <% if (topRow.length > 0) { %>
            <div class="icons-row top-row">
                <% topRow.forEach((link) => { %>
                    <% 
                    const isUPILink = link.url && (
                        link.url.toLowerCase().startsWith('upi://') ||
                        link.url.toLowerCase().startsWith('upiqr://')
                    );
                    const linkHref = isUPILink ? link.url : `/p/${user.uniqueSlug}/redirect/${link._id}`;
                    %>
                    <a href="<%= linkHref %>" 
                       class="icon-link <%= link.category %>"
                       <% if (isUPILink) { %>data-upi-link="<%= link.url %>"<% } %>>
                        <% if (link.categoryType === 'custom' && (link.icon.startsWith('data:image') || link.icon.startsWith('/'))) { %>
                            <img src="<%= link.icon %>" alt="<%= link.displayName %>">
                        <% } else { %>
                            <i class="<%= link.icon %>"></i>
                        <% } %>
                    </a>
                <% }); %>
            </div>
            <% } %>
            
            <% if (bottomRow.length > 0) { %>
            <div class="icons-row bottom-row">
                <% bottomRow.forEach((link) => { %>
                    <% 
                    const isUPILink = link.url && (
                        link.url.toLowerCase().startsWith('upi://') ||
                        link.url.toLowerCase().startsWith('upiqr://')
                    );
                    const linkHref = isUPILink ? link.url : `/p/${user.uniqueSlug}/redirect/${link._id}`;
                    %>
                    <a href="<%= linkHref %>" 
                       class="icon-link <%= link.category %>"
                       <% if (isUPILink) { %>data-upi-link="<%= link.url %>"<% } %>>
                        <% if (link.categoryType === 'custom' && (link.icon.startsWith('data:image') || link.icon.startsWith('/'))) { %>
                            <img src="<%= link.icon %>" alt="<%= link.displayName %>">
                        <% } else { %>
                            <i class="<%= link.icon %>"></i>
                        <% } %>
                    </a>
                <% }); %>
            </div>
            <% } %>
        </div>
        
        <div class="qr-download-section">
            <div class="qr-buttons">
                <% if (user.uploadedQrCode && user.uploadedQrCode.trim() !== '') { %>
                    <a href="/p/<%= user.uniqueSlug %>/download/dynamic-qr" download="dynamic-qr.png" class="download-btn">
                        <i class="fas fa-download"></i> Dynamic QR
                    </a>
                <% } %>
                <% if (user.bankQrCode && user.bankQrCode.trim() !== '') { %>
                    <a href="/p/<%= user.uniqueSlug %>/download/payment-qr" download="payment-qr.png" class="download-btn">
                        <i class="fas fa-download"></i> Payment QR
                    </a>
                <% } %>
            </div>
        </div>
        
        <script>
            document.addEventListener('DOMContentLoaded', function() {
                const upiLinks = document.querySelectorAll('a[data-upi-link]');
                
                upiLinks.forEach(function(link) {
                    link.addEventListener('click', function(e) {
                        const upiUrl = this.getAttribute('data-upi-link');
                        
                        if (!upiUrl) return;
                        
                        try {
                            window.location.href = upiUrl;
                        } catch (e) {
                            try {
                                const tempLink = document.createElement('a');
                                tempLink.href = upiUrl;
                                tempLink.style.display = 'none';
                                document.body.appendChild(tempLink);
                                tempLink.click();
                                setTimeout(function() {
                                    document.body.removeChild(tempLink);
                                }, 100);
                            } catch (e2) {
                                window.open(upiUrl, '_blank');
                            }
                        }
                    });
                });
            });
        </script>
        
        <div class="footer">
            <div class="footer-content">
                <span>Built by</span>
                <a href="https://aideveloperindia.store" class="footer-link" target="_blank">
                    <img src="/images/A-logo-transparent.png" alt="AI Developer India" class="footer-logo">
                    <span>AI Developer India</span>
                </a>
            </div>
            <div style="margin-top: 12px;">
                <a href="/terms" class="footer-link" style="font-size: 11px; opacity: 0.7;">Terms & Conditions</a>
            </div>
        </div>
    </div>
</body>
</html>
